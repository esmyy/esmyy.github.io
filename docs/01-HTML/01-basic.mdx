# 基础

<https://web.dev/learn/html/>

## Doctype

新的网页都按照标准的走就可以。

```html
<!DOCTYPE html>
```

## lang

目前普遍是使用 语言码+地区码 的值，比如 google.com.hk

```html
<html lang="zh-HK">
  <!-- ... -->
</html>
```

这并不规范，但却是事实标准，很长的一段时间内，都仍旧这样使用即可。
