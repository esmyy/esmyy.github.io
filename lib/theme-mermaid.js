import themeMermaid from "@docusaurus/theme-mermaid";
import mermaid from "mermaid";
window.sss = "sss";
if (typeof window !== "undefined") {
  window.mermaid = mermaid;
}
export default mermaid;
export default themeMermaid;